{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build && node -e \"require('dotenv').config(); const { execSync } = require('child_process'); try { execSync('npx drizzle-kit push --force', { stdio: 'inherit' }); } catch(e) { console.error('Migration failed:', e.message); }\""
  },
  "deploy": {
    "startCommand": "NODE_ENV=production node dist/index.cjs",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
